#include <stdio.h>

int main(void)
{
    int A = 8, B = 5;
    printf("A + B = %d\n", A + B); // 덧셈
    printf("A - B = %d\n", A - B); // 뺄셈
    printf("A * B = %d\n", A * B); // 곱셈
    printf("A / B = %d\n", A / B); // 나눗셈
    printf("A %% B = %d\n", A % B); // 나머지 주의: %%의 사용
    return 0;
}