#include <stdio.h>

int main(void)
{
    int A = 3, B = 1, C = 2, D = 3, E = 4;
    printf("A / B = %d \n", A / B); 
    printf("A / C = %d \n", A / C); 
    printf("A / D = %d \n", A / D); 
    printf("A / E = %d \n", A / E); 
    return 0;
}